# AI Calendar

AI Calendar is a web application workspace split into a React frontend and a Laravel backend.

Start future Codex sessions from this directory and draft requirements before scaffolding application code.
