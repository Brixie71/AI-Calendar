# Web Validation Procedures

- `integration/build-check.md`: install, format, lint, types, tests, and production build.
- `manual/smoke-test.md`: browser workflow, responsive, console/network, and accessibility checks.

Add project-native automated tests beside the application source according to its framework conventions. These Markdown files define the release procedure, not a replacement test framework.
